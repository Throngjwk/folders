var game = {
    coins:new Decimal(0),
    cps:new Decimal(0),
    cpc:new Decimal(0)
}

const app = new Vue({
    el: "#app",
    data: game,
    methods: "",
    computed: "",
    created:""
});