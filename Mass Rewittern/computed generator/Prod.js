import { Computed } from "./computed.js";
import { SuperNumbers } from "./supernumber.js";

let doc = 0;

Computed.JSON.parse("data")
Computed.JSON.parse("world")

if (Computed.JSON.parse("find") == false) {
    if (doc > 0) {
        define([
            'require',
            'documentary'
        ], function(require, factory) {
            'use strict';
            
        });
    }
} else {
    var dar = SuperNumbers.ONE
    var every = SuperNumbers.TWO
}